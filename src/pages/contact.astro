---
import BaseLayout from '../layouts/BaseLayout.astro';
import Button from '../components/button.astro';
import { sanityClient } from '../lib/sanity';
import Schema from '../components/schema.astro';

// Fetch site settings from Sanity
let siteSettings;

try {
  siteSettings = await sanityClient.fetch('*[_type == "siteSettings"][0]');
} catch (error) {
  console.log('Using fallback content (Sanity not connected yet)');
}

// Use fallback if no data returned from Sanity
if (!siteSettings) {
  siteSettings = {
    phone: "053-5227619",
    whatsapp: "972535227619"
  };
}
---

<BaseLayout title="צור קשר - השכרת פיגומים ניידים">
  <!-- Schema.org structured data -->
  <Schema type="breadcrumb" data={[
    {name: "עמוד הבית", url: "https://pigumim.netlify.app/"},
    {name: "צור קשר", url: "https://pigumim.netlify.app/contact"}
  ]} slot="head" />
  
  <!-- Header Section -->
  <section class="bg-gradient-to-br from-primary-50 via-white to-blue-50 py-16">
    <div class="container mx-auto px-4">
      <div class="text-center max-w-4xl mx-auto">
        <h1 class="text-4xl md:text-6xl font-bold mb-6 leading-tight text-gray-900">
          צור קשר
        </h1>
        <p class="text-xl md:text-2xl leading-relaxed text-gray-700">
          נשמח לעזור לכם בכל שאלה או בקשה להצעת מחיר
        </p>
      </div>
    </div>
  </section>

  <!-- Contact Methods & Form Section -->
  <section class="py-20 bg-white">
    <div class="container mx-auto px-4">
      <div class="grid lg:grid-cols-2 gap-16 max-w-6xl mx-auto">
        
        <!-- Contact Information -->
        <div class="space-y-8">
          <div>
            <h2 class="text-3xl font-bold text-gray-900 mb-8">
              דרכי התקשרות
            </h2>
            
            <!-- Phone Contact -->
            <div class="bg-gray-50 rounded-xl p-6 mb-6 hover:bg-blue-50 transition-colors">
              <div class="flex items-center mb-4">
                <div class="bg-secondary-100 rounded-full p-3 mr-4">
                  <svg class="w-6 h-6 text-secondary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
                  </svg>
                </div>
                <div class="flex-grow">
                  <h3 class="text-xl font-semibold text-gray-900">טלפון</h3>
                  <p class="text-gray-600">זמינים 24/6 לשירותכם</p>
                </div>
              </div>
              <div class="mr-16">
                <Button 
                  href={`tel:+972${siteSettings.phone?.replace(/^0/, '').replace(/-/g, '')}`}
                  variant="secondary"
                  size="md"
                >
                  📞 {siteSettings.phone}
                </Button>
              </div>
            </div>

            <!-- WhatsApp Contact -->
            <div class="bg-gray-50 rounded-xl p-6 mb-6 hover:bg-green-50 transition-colors">
              <div class="flex items-center mb-4">
                <div class="bg-green-100 rounded-full p-3 mr-4">
                  <svg class="w-6 h-6 text-green-600" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893A11.821 11.821 0 0020.885 3.488"/>
                  </svg>
                </div>
                <div class="flex-grow">
                  <h3 class="text-xl font-semibold text-gray-900">WhatsApp</h3>
                  <p class="text-gray-600">תגובה מהירה בכל שעה</p>
                </div>
              </div>
              <div class="mr-16">
                <Button 
                  href={`https://wa.me/${siteSettings.whatsapp}?text=${encodeURIComponent('שלום, אני מעוניין לקבל מידע על השכרת פיגומים ניידים. אודה לפרטים נוספים.')}`}
                  variant="outline"
                  size="md"
                >
                  שלח הודעה
                </Button>
              </div>
            </div>

            <!-- Email Contact -->
            <div class="bg-gray-50 rounded-xl p-6">
              <div class="flex items-center">
                <div class="bg-primary-100 rounded-full p-3 mr-4">
                  <svg class="w-6 h-6 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                  </svg>
                </div>
                <div class="flex-grow">
                  <h3 class="text-xl font-semibold text-gray-900">טופס יצירת קשר</h3>
                  <p class="text-gray-600">מלא את הטופס ונחזור אליך</p>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Contact Form -->
        <div class="bg-gradient-to-br from-gray-50 to-blue-50 rounded-2xl p-8">
          <h2 class="text-3xl font-bold text-gray-900 mb-8 text-center">
            שלח לנו הודעה
          </h2>
          
          <form 
            name="contact" 
            method="POST" 
            data-netlify="true" 
            data-netlify-honeypot="bot-field"
            class="space-y-6"
          >
            <!-- Netlify form detection -->
            <input type="hidden" name="form-name" value="contact" />
            
            <!-- Honeypot field for spam protection -->
            <div class="hidden">
              <label>Don't fill this out if you're human: <input name="bot-field" /></label>
            </div>

            <!-- Name Field -->
            <div>
              <label for="name" class="block text-sm font-semibold text-gray-900 mb-2">
                שם מלא *
              </label>
              <input 
                type="text" 
                id="name" 
                name="name" 
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent text-right bg-white"
                placeholder="הכנס את שמך המלא"
              />
            </div>

            <!-- Phone Field -->
            <div>
              <label for="phone" class="block text-sm font-semibold text-gray-900 mb-2">
                טלפון *
              </label>
              <input 
                type="tel" 
                id="phone" 
                name="phone" 
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent text-right bg-white"
                placeholder="050-123-4567"
              />
            </div>

            <!-- Email Field -->
            <div>
              <label for="email" class="block text-sm font-semibold text-gray-900 mb-2">
                אימייל
              </label>
              <input 
                type="email" 
                id="email" 
                name="email"
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent text-right bg-white"
                placeholder="your.email@example.com"
              />
            </div>

            <!-- Service Type -->
            <div>
              <label for="service" class="block text-sm font-semibold text-gray-900 mb-2">
                סוג השירות המבוקש
              </label>
              <select 
                id="service" 
                name="service"
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent text-right bg-white"
              >
                <option value="">בחר סוג שירות</option>
                <option value="פיגום אלומיניום סטנדרטי 6.3 מ">פיגום אלומיניום סטנדרטי 6.3 מ</option>
                <option value="פיגום סטנדרטי 4.6 מ">פיגום סטנדרטי 4.6 מ</option>
                <option value="פיגום אלומיניום מקצועי רוחב כפול">פיגום אלומיניום מקצועי רוחב כפול</option>
                <option value="ייעוץ והדרכה">ייעוץ והדרכה</option>
                <option value="אחר">אחר</option>
              </select>
            </div>

            <!-- Message Field -->
            <div>
              <label for="message" class="block text-sm font-semibold text-gray-900 mb-2">
                הודעה *
              </label>
              <textarea 
                id="message" 
                name="message" 
                rows="5"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent text-right resize-y bg-white"
                placeholder="ספר לנו על הפרויקט שלך, התאריכים הנדרשים וכל פרט רלוונטי אחר..."
              ></textarea>
            </div>

            <!-- Submit Button -->
            <div class="pt-4">
              <Button 
                type="submit"
                variant="secondary"
                size="lg"
                class="w-full"
              >
                שלח הודעה
              </Button>
            </div>

            <!-- Privacy Notice -->
            <p class="text-xs text-gray-600 text-center leading-relaxed">
              הפרטים שלכם מוגנים ולא יועברו לצד ג'. נחזור אליכם בהקדם האפשרי.
            </p>
          </form>
        </div>
      </div>
    </div>
  </section>

  <!-- Quick Contact CTA -->
  <section class="py-16 bg-gradient-to-br from-primary-50 via-white to-blue-50">
    <div class="container mx-auto px-4 text-center">
      <h2 class="text-3xl font-bold text-gray-900 mb-6">
        צריכים תשובה מהירה?
      </h2>
      <p class="text-lg text-gray-700 mb-8 max-w-2xl mx-auto">
        לשירות מהיר ויעיל, התקשרו אלינו ישירות או שלחו הודעה בוואטסאפ
      </p>
      
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <Button 
          href={`tel:+972${siteSettings.phone?.replace(/^0/, '').replace(/-/g, '')}`}
          variant="secondary"
          size="lg"
        >
          📞 התקשר עכשיו
        </Button>
        <Button 
          href={`https://wa.me/${siteSettings.whatsapp}`}
          variant="outline"
          size="lg"
        >
          💬 WhatsApp
        </Button>
      </div>
    </div>
  </section>

</BaseLayout>

<style>
  /* RTL form styling improvements */
  input[type="text"], 
  input[type="tel"], 
  input[type="email"], 
  select, 
  textarea {
    direction: rtl;
  }
  
  input::placeholder, 
  textarea::placeholder {
    text-align: right;
    direction: rtl;
  }

  /* Focus styles for accessibility */
  input:focus, 
  select:focus, 
  textarea:focus {
    outline: none;
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
  }
</style>